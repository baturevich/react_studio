import React from 'react'
import { connect } from 'react-redux'
import MainPage from './MainPage'

const MainPageContainer = (props)=>{
    return(
        <MainPage  />
    )
}
export default connect(null,null)(MainPageContainer)